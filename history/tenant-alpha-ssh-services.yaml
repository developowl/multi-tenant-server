# %%%%%%%%%%%%%%%%%%%%%%%%%%
# 현재 사용하지 않는 정의 파일입니다.
# %%%%%%%%%%%%%%%%%%%%%%%%%%

apiVersion: v1
kind: Service
metadata:
  name: tenant-alpha-ssh
  namespace: tenant-alpha
  annotations:
    metallb.universe.tf/loadBalancerIPs: ****
spec:
  type: LoadBalancer
  selector:
    role: server
  ports:
  - name: ssh
    protocol: TCP
    port: ****         
    targetPort: ****   
