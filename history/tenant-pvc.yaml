# %%%%%%%%%%%%%%%%%%%%%%%%%%
# 현재 사용하지 않는 정의 파일입니다.
# %%%%%%%%%%%%%%%%%%%%%%%%%%

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tenant-alpha-pvc
  namespace: tenant-alpha
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

